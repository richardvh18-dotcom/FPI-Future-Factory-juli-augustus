import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.futurefactory.fpi',
  appName: 'Future Factory FPI',
  webDir: 'dist'
};

export default config;
